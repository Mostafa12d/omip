build/
build*/
# Generated by thirdparty/bfl/CMakeLists.txt's configure_file() at build
# time; must land inside the vendored source tree (see that file's
# comments), so it needs a dedicated ignore rule rather than a build/ dir.
thirdparty/bfl/orocos_bfl/src/wrappers/config.h
tests/fixtures/**/*.npz
